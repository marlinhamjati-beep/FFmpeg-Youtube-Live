# FFmpeg YouTube Live

## Cara pakai:
1. Download FFmpeg dan pastikan `ffmpeg.exe` ada di PATH atau folder yang sama dengan `start-live.bat`.
2. Buka `config.json`, ganti `stream_key` dengan Stream Key YouTube kamu.
3. Jalankan `start-live.bat` untuk mulai live streaming.
